# 桌面整理小工具 — 环境变量示例
# 复制本文件为 .env 并填入实际值，程序启动时会自动加载项目根目录下的 .env。
# 也可直接在系统中设置以下变量。

# 智能分类（可选）：启用「规则未命中时用 Qwen 向量推荐目标」时，DashScope API Key。
# 获取：https://help.aliyun.com/zh/model-studio/get-api-key
# 未设置时智能分类不调用接口，仅使用规则与反馈库。
# DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxx
